[package]
name = "genpack"
version = "0.1.0"
edition = "2021"

[features]
default = ["verify"]
verify = []
choco = []
brew = []
apt = []
dnf = []
yum = ["dnf"]


[dependencies]

[[test]]
name = "basic"
required-features = ["verify", "choco", "brew", "apt", "dnf", "yum"]
